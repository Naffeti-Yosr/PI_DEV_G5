<?xml version="1.0" encoding="UTF-8"?>

<?import java.net.*?>
<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<AnchorPane prefHeight="600" prefWidth="900" styleClass="root" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.esprit.controllers.ForgetPasswordController">
    <stylesheets>
        <URL value="@styles/energy.css" />
    </stylesheets>

    <GridPane AnchorPane.bottomAnchor="0" AnchorPane.leftAnchor="0" AnchorPane.rightAnchor="0" AnchorPane.topAnchor="0">
        <columnConstraints>
            <ColumnConstraints percentWidth="40" />
            <ColumnConstraints percentWidth="60" />
        </columnConstraints>

        <!-- Left Panel -->
        <VBox alignment="CENTER" prefHeight="800.0" prefWidth="1200.0" spacing="30" styleClass="welcome-panel" GridPane.columnIndex="0">
            <padding>
                <Insets bottom="50" left="40" right="40" top="50" />
            </padding>

            <Text styleClass="app-title" text="Mot de passe oublié" />
            <Text styleClass="welcome-text" text="Récupérez votre mot de passe" />
            <Text styleClass="slogan-text" text="Pour une énergie plus verte" />

            <Button onAction="#handleBackToLogin" styleClass="back-button" text="Retour à la connexion" />
        </VBox>

        <!-- Right Panel - Forget Password Form -->
        <VBox spacing="25" styleClass="register-panel" GridPane.columnIndex="1">
            <padding>
                <Insets bottom="40" left="50" right="50" top="40" />
            </padding>

            <Text styleClass="form-title" text="Mot de passe oublié" />

            <GridPane hgap="20" vgap="20">
                <columnConstraints>
                    <ColumnConstraints percentWidth="100" />
                </columnConstraints>

                <!-- Email -->
                <VBox spacing="8" GridPane.columnIndex="0" GridPane.rowIndex="0">
                    <Label styleClass="input-label" text="Email" />
                    <TextField fx:id="tfEmail" promptText="exemple@email.com" styleClass="modern-input" />
                </VBox>
            <rowConstraints>
               <RowConstraints />
            </rowConstraints>
            </GridPane>

            <Button fx:id="btnSubmit" maxWidth="Infinity" onAction="#handleSubmit" styleClass="register-submit-button" text="Envoyer" />

            <VBox alignment="CENTER" spacing="5">
                <Label styleClass="footer-text" text="powered by solaris.org" />
                <Label styleClass="version-text" text="Version 0.5" />
            </VBox>
        </VBox>
      <rowConstraints>
         <RowConstraints />
      </rowConstraints>
    </GridPane>
</AnchorPane>
